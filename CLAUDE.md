# CLAUDE.md

## !! 最優先指示 — セッション開始時に必ず実行すること !!

**リポジトリの探索・git log・ファイル確認を行う前に、必ず以下を実行すること。**

### Memory.md の取得手順

GitHub MCP ツールで以下のファイルを取得する：

```
owner: yamamori338454
repo:  claude
branch: notes/main
path:  notes/Memory.md
```

取得後、Memory.md の内容に従って学習を継続する。

> このリポジトリの main ブランチには IT学習とは無関係のファイルが含まれているが、無視してよい。
> ノート・学習データはすべて `notes/main` ブランチにある。

---

## Communication

- 日本語で対話すること

## ノートの保存ルール

- ノートの push 先ブランチは **常に `notes/main`** を使うこと
- セッションごとに割り当てられる開発ブランチ（`claude/...`）には push しない
- セッション終了後、`notes/Memory.md` のセッション履歴と次回やることを更新して `notes/main` に push すること

## Project Status

IT学習プロジェクト — ネットワーク基礎フェーズ進行中。

## Development Commands

*To be defined once the project structure is established.*

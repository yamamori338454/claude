# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Communication

- 日本語で対話すること

## 引き継ぎ手順（セッション開始時）

セッション開始時は必ず以下の手順で引き継ぎを行うこと：

1. `notes/Memory.md` をローカルで探す
2. 存在しない場合は GitHub MCP ツールで取得すること：
   - owner: `yamamori338454`
   - repo: `claude`
   - branch: `notes/main`
   - path: `notes/Memory.md`
3. Memory.md の内容に従って学習を継続する

## ノートの保存ルール

- ノートの push 先ブランチは **常に `notes/main`** を使うこと
- セッションごとに割り当てられる開発ブランチ（`claude/...`）には push しない
- セッション終了後、`notes/Memory.md` のセッション履歴と次回やることを更新して `notes/main` に push すること

## Project Status

IT学習プロジェクト — ネットワーク基礎フェーズ進行中。

## Development Commands

*To be defined once the project structure is established.*
